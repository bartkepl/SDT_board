<UserControl x:Class="SDT_App.Views.ConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:vm="clr-namespace:SDT_App.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:ConnectionViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10" MaxWidth="700">

            <!-- VISA Resource -->
            <GroupBox Header="VISA Resource" Margin="0,0,0,8">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox Grid.Column="0" ItemsSource="{Binding Resources}"
                                  SelectedItem="{Binding SelectedResource}"
                                  mah:TextBoxHelper.Watermark="Select a VISA resource…"/>
                        <Button Grid.Column="2" Command="{Binding RefreshCommand}"
                                ToolTip="Refresh resource list" Width="36">
                            <iconPacks:PackIconMaterial Kind="Refresh" Width="14" Height="14"/>
                        </Button>
                    </Grid>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="Connect"
                                Command="{Binding ConnectCommand}"
                                Style="{StaticResource MahApps.Styles.Button.Square.Accent}">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="Power" Width="13" Height="13" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                        <TextBlock Text="Connect" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>
                        <Button Grid.Column="2" Content="Disconnect"
                                Command="{Binding DisconnectCommand}">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial Kind="PowerOff" Width="13" Height="13" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                        <TextBlock Text="Disconnect" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>
                    </Grid>

                    <Border Grid.Row="4" CornerRadius="3" Padding="8,4"
                            Background="{DynamicResource MahApps.Brushes.ThemeBackground}">
                        <TextBlock Text="{Binding StatusText}" FontSize="12">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                            <Setter Property="Foreground" Value="#44BB66"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsConnected}" Value="False">
                                            <Setter Property="Foreground" Value="#EE8844"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Border>
                </Grid>
            </GroupBox>

            <!-- Device Info -->
            <GroupBox Header="Device Information" Margin="0,0,0,8">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="6"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="6"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="6"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="*IDN?" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding IdnText}"
                               FontFamily="Consolas" FontSize="11" VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                    <Button Grid.Row="0" Grid.Column="3" Content="Query"
                            Command="{Binding QueryIdnCommand}"
                            IsEnabled="{Binding IsConnected}"/>

                    <Separator Grid.Row="1" Grid.ColumnSpan="4"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="SCPI Version" VerticalAlignment="Center"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding VersionText}"
                               FontFamily="Consolas" FontSize="11" VerticalAlignment="Center"/>
                    <Button Grid.Row="2" Grid.Column="3" Content="Query"
                            Command="{Binding QueryVersionCommand}"
                            IsEnabled="{Binding IsConnected}"/>
                </Grid>
            </GroupBox>

            <!-- Tips -->
            <GroupBox Header="Quick Tips">
                <StackPanel Margin="8" Opacity="0.75">
                    <TextBlock Text="• Click Refresh to discover VISA instruments on USB" Margin="0,2"/>
                    <TextBlock Text="• USB-TMC resources appear as: USB0::0xVID::0xPID::SN::INSTR" Margin="0,2"
                               FontFamily="Consolas" FontSize="11"/>
                    <TextBlock Text="• Connect will query the sensor type automatically" Margin="0,2"/>
                    <TextBlock Text="• Requires NI-VISA or Keysight IO Libraries Suite installed" Margin="0,2"/>
                </StackPanel>
            </GroupBox>

        </StackPanel>
    </ScrollViewer>
</UserControl>
