<UserControl x:Class="SDT_App.Views.ConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:vm="clr-namespace:SDT_App.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:ConsoleViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Input -->
        <GroupBox Grid.Row="0" Header="SCPI Command">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="6"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="4"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="CommandBox"
                         Grid.Row="0" Grid.Column="0"
                         Text="{Binding CommandText, UpdateSourceTrigger=PropertyChanged}"
                         FontFamily="Consolas" FontSize="13"
                         IsEnabled="{Binding IsConnected}"
                         mah:TextBoxHelper.Watermark="Enter SCPI command (e.g. *IDN? or DISPlay:STATe 1)..."
                         KeyDown="OnCommandKeyDown"/>
                <Button Grid.Row="0" Grid.Column="2"
                        Command="{Binding SendCommand}"
                        IsDefault="True"
                        Style="{StaticResource MahApps.Styles.Button.Square.Accent}"
                        Width="80">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Send" Width="13" Height="13" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBlock Text="Send" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Grid.Row="0" Grid.Column="4" Content="Clear Output"
                        Command="{Binding ClearOutputCommand}"
                        Width="90"/>

                <TextBlock Grid.Row="2" Grid.ColumnSpan="5"
                           Opacity="0.6" FontSize="11"
                           Text="Up/Down = history  |  Commands ending with ? are queried; others are written"/>
            </Grid>
        </GroupBox>

        <!-- Output -->
        <GroupBox Grid.Row="2" Header="Response / Output">
            <ListBox x:Name="OutputList"
                     ItemsSource="{Binding OutputLines}"
                     FontFamily="Consolas" FontSize="12"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     VirtualizingPanel.IsVirtualizing="True">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="2,1"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Text}" TextWrapping="NoWrap"
                                   Foreground="{Binding Type, Converter={StaticResource ConsoleEntryTypeToColor}}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

    </Grid>
</UserControl>
